# Branches Git

| Branche   | Usage                          |
|-----------|--------------------------------|
| **codex** | Travail fait avec **Codex**    |
| **cursor**| Travail fait avec **Cursor**   |

Changer de branche : `git checkout codex` ou `git checkout cursor`.
